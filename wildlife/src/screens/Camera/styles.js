import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  input: {
    padding: 5,
    fontSize: 20,
    borderWidth: 1,
    width: "40%",
  },
});

export default styles;
