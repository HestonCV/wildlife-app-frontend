import React from "react";
import { SafeAreaView, Text } from "react-native";

const Camera = () => {
  return (
    <SafeAreaView>
      <Text>Camera</Text>
    </SafeAreaView>
  );
};

export default Camera;
